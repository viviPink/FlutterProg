name: calc_abc
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^7.2.0
  sqflite: ^2.0.0+4 # Для работы с SQLite
  path_provider: ^2.0.11 # Для получения пути к директории приложения

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
